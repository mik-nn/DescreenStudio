# Journal

Хронология решений, экспериментов и прогресса. Формат записи — см. `WORKFLOW.md`. Новые записи добавляются **сверху**.

---

## 2026-09-14 — 1.8 DONE: P1-симулятор (LPI/tint/banding/красочность/show-through); FM WONTFIX

**Контекст:** Калибровка по 14 реальным сканам; решение пользователя: FM нецелесообразен (муар на стохастике физически невозможен, в материале 0/14); ICC — только для синтетики, дефолт ICC.

**Решение/Результат:**
- **FM → WONTFIX** (обратимо): муар требует периодичности обеих структур; FM-зерно — не муар; целевая полиграфия — AM. Долг из плана §3.2 закрыт явно, а не висит.
- **Роль ICC зафиксирована:** дефолт сепарации для синтетики — ICC-пресеты (уже в конфиге); наивный путь — только fallback; к реальным сканам ICC не применяется никогда.
- Код (`config/scan/compose/generate`): LPI `magazine 120–160 / newspaper 75–100 / fine 150–190`; `paper_tint` (белый 70/70/50%, иначе желтизна B до 0.85); `band_lpi 6–10 / amp 0–0.02 / ось / фаза` в scan-каскаде после даунсэмпла (период масштабируется от scan_dpi); `ink_set cmyk(0.8)/k(0.1)/duotone(0.1)` — K по люме 45°, дуотон K@45 + spot@75°; show-through (`SHOWTHROUGH_PROB 0.15`, alpha 0.005–0.04, mirrored+blurred ghost после даунсэмпла); всё пишется в манифест (ink_set, tint, ghost_src, band/show_alpha).
- Тест: все ветви без падений; пики ровно r=LPI/DPI (0.464 @140/300); banding даёт когерентный пик xmean=85 на расчётной частоте (метрика-замечание: energy-sum по широкой полосе врёт из-за пуассоновской связи — мерить пиком); show-through сдвиг −0.0162 vs расчёт −0.0141 ✓; `make_patch` ×3 (cmyk/duotone/k, tint до B 0.864, ghost сработал 1/3 с alpha 0.0263).

**Артефакты:** `ml/simulator/{config,scan,compose}.py`, `ml/dataset/generate.py`; задача 1.8 → DONE.

**Следующий шаг:** перенос E1+bgswap+lintone в Rust `run_descreen`; затем спека run4.

---

## 2026-09-14 — Обзор 14 реальных сканов: калибровка симулятора P1

**Контекст:** Вечерние сканы (12 новых: книги, календари, журналы, офсет, упаковка; 1200 DPI, два по 800/600). Замер центральных ROI 1024 + позонная проверка подозрительных.

**Решение/Результат (r → LPI = r×DPI):**
- 119 LPI: 1821145/1821146 (800 DPI), CoverOldBook (600), OldBook (сильный, 4.6×thr, ±135°).
- 136–154: book1 (136, +3-я гармоника 0.34), Offset1–4 (154, эталонные ±45°, +√2-гармоника).
- 161–178: Calendar1 (161), Calendar2 (161/178 — центр пустой!), BookCOver (169), Magazine (170), Magazine (2) (169 + **второй растр 87** + странный 0.244), Pack (174 + точная √2-гармоника).
- Magazine: два близких растра 170 + 185 (каналы?).
- Углы — стандартные CMYK-семейства; симуляторные 15/75/0/45 + джиттер 3° адекватны.
- FM не найден нигде — всё AM (включая флексо-упаковку). FM-долг остаётся открытым (цифровая печать).
- Offset4: осевые НЧ-полосы 0.015–0.019 — подтверждение paper banding для P1.
- Метод-урок: центр ROI падает на пустые поля (Calendar2/Offset4 var~0.001, найквист-мусор) — для картирования использовать `raster_map` (мультипатч), не центр.
- Дельты симулятора: LPI 85–175 → **75–190**; добавить paper banding (осевое, r~0.01); mixed-screen патчи (169+87) — P2; 3-и гармоники бинарных точек — уже есть естественно.
- **Уточнение от пользователя:** все 4 Offset — один печатный лист с двух сторон → условие 154 LPI едино (подтверждено спектрами); кандидат-измерение: просвечивание оборота (show-through) как будущая аугментация. Calendar — печать в **2 краски (чёрная + серая)**: симулятору нужно измерение «красочность» {CMYK, K-only, дуотон K+spot} — сейчас всегда CMYK; дуотон — в дизайн P1-апгрейда (угол второй плашки свой).

**Артефакты:** `Data/RealScan/*.tif` (gitignored); задача 1.8 → IN PROGRESS.

**Следующий шаг:** внести LPI-диапазоны + paper banding в симулятор; спека run4; перенос E1+bgswap в Rust — по решению.

---

## 2026-09-14 — 1.7 DONE: ICC-профили в симуляторе (контекстная сепарация)

**Контекст:** Наивный `rgb_to_cmyk` заменён контекстными ICC-профилями: ISO_Coated, ISO_Uncoated, ISO Uncoated Yellowish, PSO SNP Paper.

**Решение/Результат:**
- Скачан ECI Offset 2009 (`eci.org/lib/exe/eci_offset_2009.zip`, 15.7 МБ, 2009-05-29) во временную папку; отобраны 4 профиля (~7 МБ — вне git по правилам репо, как DLL/модель): `ISOcoated_v2_eci.icc` (FOGRA39), `PSO_Uncoated_ISO12647_eci.icc` (FOGRA47), `ISOuncoatedyellowish.icc` (FOGRA30), `PSO_SNP_Paper_eci.icc` (FOGRA42) → `ml/simulator/icc/` + `manifest.json` (sha256, источник, маппинг); `.gitignore` (`icc/*.icc`); `scripts/fetch_icc_profiles.ps1` для восстановления.
- `config.py`: поле `icc_profile` + `icc_intent` (0=perceptual) в `PrintConfig`, `PRESET_ICC` (newspaper→SNP, magazine/fine→ISOcoated; uncoated — под явный override); `color.py`: `rgb_to_cmyk_icc` (sRGB→CMYK через ImageCms, perceptual) + диспетчер `separate_rgb` (None → наивный; отсутствующий файл → FileNotFoundError с подсказкой про fetch-скрипт); `compose.py` переведён на диспетчер в обоих путях.
- Тест: ICC даёт K 0.03–0.12 против наивных 0.25 (настоящий GCR), профили различаются между собой (SNP K 0.11 vs uncoated 0.079/0.033); `simulate_print` обоих пресетов — диапазон [0,1], пики ровно r=LPI/DPI (0.0554/0.0396). Обратная совместимость: `icc_profile=None` → наивный путь.

**Артефакты:** `ml/simulator/{color,config,compose}.py`, `icc/manifest.json`, `scripts/fetch_icc_profiles.ps1`; задача 1.7 → DONE.

**Следующий шаг:** P1-остаток (FM-растр, тон бумаги, НЧ-полосы в симулятор); вечерние фото; спека run4.

---

## 2026-09-14 — Спека run4: архитектура, лосс, данные, приёмка

**Контекст:** Классика упёрлась (остатки 64/88 в средних тонах). Run4 бьёт по измеренным гэпам run3: остатки на границах, высветление +0.10, съеденные детали. База: run3 = base96/L3/enc(1,1)/bneck3/dec(1,1)/spec(F,T,T), 8 эпох, batch 4, lr 5e-5, GPU RTX 3080 Ti Laptop 16 ГБ.

**Решение/Результат (спека, не запуск):**
- **Архитектура (дельты):** R4-A1 `spectral=(T,T,T)` — FFC на L0, где живёт растр 6.7px (флаг `arch_spectral_l0` уже есть, код не трогаем); R4-A2 углубление residual-стека enc(2,2)/bneck4/dec(2,2) (~5.5M парам., batch 4 должен влезть, fallback 3) — FFCBlock уже residual (ответ на запрос ResNet); R4-A3 DCT-входы (3→5ch) — ВЫКЛ по умолчанию (требует зеркала в Rust prefilter; только абляцией); R4-A4 FiLM-conditioning на замер A0 — фаза 2.
- **Лосс:** edge-weighted L1 (вес 1+α·|∇GT|, α~2) + tone-loss (w~0.5, |mean| поканально — лечит +0.10 в корне) + MS-SSIM 0.5 / Fourier 0.1 / LPIPS 0.1 (реализация: новые поля `LossConfig` + флаги `train.py`).
- **Данные (regen!):** новый `Data/synth_run4` (старый не трогаем): P1-сим (ICC-дефолт, LPI 75–190, inks 0.8/0.1/0.1, tint, banding, show-through) + edge-oversampling (дописать `edge_q` в манифест generate.py, boost Q4×2 к существующим); 10k/1k; пилот 200 шт для замера времени.
- **Обучение:** с нуля (warm-start несовместим), seed 44, lr 5e-5 cosine, AMP+EMA; smoke оверфит 20 шагов (прецедент 0.75→0.40).
- **Приёмка двойная:** синтетика val PSNR>32.88/SSIM>0.907 + RealScan no-GT (resid<40, sharp 0.9–1.15, |DC|<0.005, edgeHF<1.6, LF≤1.0, кропы). Недобор → фаза 2 (A3/A4) или возврат к данным.
- **Экспорт:** штатный пайплайн Этапа 3 (FFT-часть FFC не менялась); паритет-проверка обязательна.

**Артефакты:** задача 2.11 TODO (спека утверждена обсуждением 14.09).

**Следующий шаг:** по команде — пилот генерации (200 шт) → полный regen → обучение run4.

---

## 2026-09-17 — DCT-входы: паритет 6e-7, R4'' запущен

**Контекст:** Решение «сначала DCT». Дизайн: блок-DCT 8×8 ortho, полосы screen 0.08–0.28 / hi ≥0.28, log-энергии, nearest-апскейл, каналы [R,G,B,E_S,E_H].

**Решение/Результат:**
- `ml/train/dctfeat.py` (контракт в docstring) + `DescreenDataset(..., extra_dct)` (GT остаётся 3ch) + флаг `--extra-dct`; `unet.py`: residual только из RGB (`x[:, :3]`, поймано первым же запуском — shape mismatch 5 vs 3).
- Rust: `prefilter::dct_channels` (сепарабельный косинус-матрикс) + диспетчер в `InferenceEngine::new` (каналы входа из `session.inputs()[0]`, 3|5, иначе ошибка) + `process_tile` доклеивает DCT к тайлу (RGB-конвейер не тронут — tiling/accumulate 3ch).
- Паритет PY/Rust: maxd **6e-7** (по пути поймана ошибка раскладки в самом тесте: (H,W,2) vs (2,H,W), не в коде). Временный live-тест удалён, остался структурный (naive f64 + DC-ноль). 7/7 Rust-тестов зелёных.
- R4'' запущен: spectral(F,T,T) + enc2/bneck4 + edge1.0/tone0.5 + P1 + DCT, seed 45, 8 эпох. Движок сам определит 5ch по файлу модели после экспорта.

**Артефакты:** `ml/{train/dctfeat.py,model/unet.py,train/train.py,train/data.py}`, `src-tauri/src/{prefilter,inference}.rs`; задача 2.11 → IN PROGRESS (R4'').

**Следующий шаг:** следить TB/логи; по готовности — экспорт 5ch + приёмка.

---

## 2026-09-17 — run4: диагноз 59с/шаг; L0-спектр выведен из спеки (дорого)

**Контекст:** Ночь: 531 шаг за 8.68ч (59с/шаг), затем ран молча умер (процессы исчезли, GPU в idle). Run3 делал 0.72с/шаг на той же машине.

**Решение/Результат (замеры на чистом GPU):**
- Изолированно: run4-arch 85с/шаг (fp32) vs run3-arch 8.7с — сама архитектура тяжелее на порядок, не окружение. Лоадер оправдан (батч 0.1с), диск/Defender/TB — мимо.
- Виновник: 4 полноразмерных FFC-блока на L0 (rFFT/iFFT 96ch×512²×batch4 + зеркала/index_select/cat) — memory-bound трафик комплексных тензоров (~ГБ/блок), моя оценка ~1с/блок была неверна в ~10 раз. Углубление даёт лишь ~1.7×, edge/tone-лоссы — копейки.
- AMP тут почти не помогает (59с наблюдённых vs 85с fp32 = 1.4×): FFC принудительно fp32 (фикс корректности 6eb6251) — а run3 жил именно за счёт AMP (8.7→0.72, 12×).
- Forward-only (ответ про инференс): run3 0.212с vs run4 0.537с (batch 4, AMP) — всего 2.5×. Мегамедленного инференса НЕ будет: в продукте нет лосса/backward/оптимизатора, батч 1, DFT всё равно на CPU-fallback. Ожидание run4-продукта ~2–4× от 2.6с/тайл (A4 за ~4–8 мин) — тот же порядок.
- Урок: бенчи рядом с обучением на забитых 16 ГБ, видимо, и убили ночной ран (память) — больше так не делать; плюс пайп-Sеlect-Object теряет вывод убитых процессов (все «зависания» лоадера/бенчей были артефактами).
- Ревизия спеки: L0-спектр УБРАН (нежизнеспособно: 59с×20k = ~14 суток). R4' = глубина enc(2,2)/bneck4 + edge/tone-loss + P1-данные + spectral(F,T,T); темп ≈ run3 (~1с/шаг, ~6–7ч). Частоту на full-res добираем DCT-входами (нулевая цена GPU) или фазой 2.

**Артефакты:** `Temp/opencode/bench_{arch,arch2,amp,fwd,clean}.py` (замеры); задача 2.11 → ревизия спеки.

**Следующий шаг:** решение пользователя — перезапуск R4' (без L0) или альтернативы.

---

## 2026-09-14 — run4 запущен: код, smoke, обучение

**Контекст:** Спека 2.11, данные synth_run4 готовы (10k/1k, манифест полный).

**Решение/Результат:**
- `losses.py`: edge-weighted L1 (`w_edge`, вес 1+α·|∇GT|/mean, α=2) + tone-loss (w~0.5, |mean| поканально); tb-логи `train/edge`, `train/tone`.
- `train.py`: флаги `arch-enc/dec-blocks`, `edge-w/alpha`, `tone-w`, плюс недостававшие `--data`/`--seed`; `hard_weights` расширен edge-Q4×2 (sidecar `edge_q.json` по 10k GT — `ml/dataset/edge_q.py`).
- Smoke-оверфит (arch run4, 20 шагов): total 0.708→0.420, edge 0.148→0.078, tone 0.086→0.010 — все компоненты учатся.
- Запуск: `--data Data/synth_run4 --out ml/train/_run4 --epochs 8 --batch 4 --lr 5e-5 --arch-base 96 --bottleneck 4 --enc/dec 2 --specL0 1 --edge 1.0/2.0 --tone 0.5 --hard-boost 1 --seed 44`. По пути пойман баг: в CLI не было `--seed` (первый старт упал до обучения). GPU 100% / 16 ГБ, лог `Temp/opencode/train_run4.log`, TensorBoard :6006 подхватит сам.

**Артефакты:** `ml/model/losses.py`, `ml/train/train.py`, `ml/dataset/edge_q.py`, `ml/train/_run4/`; задача 2.11 → IN PROGRESS.

**Следующий шаг:** следить лог/TensorBoard; по готовности — экспорт+приёмка (синтетика + RealScan-таблица).

---

## 2026-09-14 — Regen synth_run4 запущен (12 воркеров, ~3ч)

**Контекст:** Пилот P1 (200 шт, 9.7с/шт) валиден. `generate.py` получил `--root` (старый `Data/synth` не трогаем).

**Решение/Результат:**
- Smoke `--root Data/synth_smoke` (2+1) — ок, удалён.
- Запущен полный regen: `generate.py --root Data/synth_run4 --train 10000 --val 1000 --seed 44 --workers 12`, лог `Temp/opencode/regen_run4.log`. Оценка ~3ч.
- Урок: фоновые процессы переживают убийство шелла harness'ом (пилот добежал так же); проверять по логу/счётчику файлов.

**Следующий шаг:** по готовности regen — обучение run4 по спеке 2.11.

---

## 2026-09-14 — Схемы архитектуры + пилот P1-генерации DONE + §2 плана обновлён

**Контекст:** Запрос: нарисовать архитектуру сети и пайплайна со всеми обсуждёнными плюшками (A0, ICC и др.). Плюс перезапуск убитого пилота.

**Решение/Результат:**
- `knowledge/pipeline.png`: три дорожки — A. синтез P1 (GT→ICC→AM→бумага→скан→synth_run4), B. замер A0 (контраст→пики→LPI/углы/conf→notch + FiLM фаза 2), C. инференс (A0→адаптивный notch→LF→run3→бленд→bg-swap→tone); боксы сети run3/run4-дельт и метрик без GT. `knowledge/network.png`: энкодер/декодер base96/L3, FFC-инсет, лосс и данные run4; красным — больные места run3 (нет спектра на L0).
- Пилот перезапускать не пришлось: убитая сессия успела добежать — 200/200 за 1943с (9.7с/патч single-worker; полный regen 11k ≈ 30ч/1 воркер ≈ 2.5–3ч на 12 воркерах). Манифест полный (пресеты/inks/ghost 19%/tint/band-параметры); спектры 3 случайных патчей — растровые пики xmean 90–110. P1-конвейер валиден целиком.
- §2 `ImplementationPlan.md` переписан под трёхдорожечную архитектуру со ссылками на PNG-схемы (старая двухблочная схема устарела: в ней не было A0, ICC, bg-swap).

**Артефакты:** `knowledge/{pipeline,network}.png`; `Data/synth_pilot/` (gitignored); задача: пилот → DONE.

**Следующий шаг:** полный regen `Data/synth_run4` (12 воркеров, ночь) → обучение run4 по спеке 2.11.

---

## 2026-09-14 — 2.10 DONE: сглаживание карт убило лоскутность; перф notch ×27

**Контекст:** Жалобы: лоскутная сшивка в тенях, усиленный растр в тенях, инверсия в светах. Замер: сеток 64–512 нет ни глобально, ни локально (шаги на линиях = медиане) — «сшивка» была лоскутностью карт (ступеньки радиусов + nearest-блоки весов бленда); белые точки в светах наполовину новые (48% overlap с исходником).

**Решение/Результат:**
- Сглаживание карт (PY+Rust): blur σ=1 кл. сетки радиусов; edge01 — bilinear + blur вместо nearest. Лоскутность ушла (кропы).
- Перф: триг вынесен из попиксельного цикла + кэш масок по 8 уровням гейна: notch 76с→1.9с на 2048² (кроп), префильтр полного кадра ~10с.
- Финал Rust (оба кадра): sharp 0.92/1.29, resid 64/89, LF 0.90/0.68, DC ~0; края 1.82×/1.34× без рингинга. В средних тонах точки остались — предел классики при текущей силе (дальше run4 или сильнее notch ценой деталей).
- Попутно: постоянные `[timing]`-замеры стадий в `descreen_file`; `DESCREEN_STAGE_DBG` оставлен.

**Артефакты:** `ml/preproc/{edgenotch,blend}.py`, `src-tauri/src/{prefilter.rs,app.rs}`; задача 2.10 → DONE.

**Следующий шаг:** спека run4 — классика упёрлась в компромисс сила/детали.

---

## 2026-09-14 — 2.9 DONE: Gaussian-notch + blend-guards; зерно в плашках — предел классики

**Контекст:** Жалоба: усиленный растр на границах, общая контрастность, убитые света. Замер по зонам: полоса растра везде задавлена (0.13–0.27×), а широкополосных ВЧ мы ДОБАВЛЯЕМ — у границ 2.32× (рингинг широкого Баттерворта + зерно сети).

**Решение/Результат:**
- Gaussian-notch (та же −3dB ширина, без боковых лепестков) + кап `edge_gain` 2.0→1.0; измерение: pre-стадии идентичны (края 0.78/0.79×) — зерно добавляет сеть, не notch.
- Адаптивный бленд: защита светов (0.75–0.95), теней (0.25–0.05, ×0.5), границ (edge_guard 0.7, сетка p128 — оптимум; p64 не лучше; пиксельный гард отвергнут измерением: травится самими точками растра, хуже).
- Зонально: края HF 2.88×→2.25× (кроп), полные кадры 1.82×/1.34×; flat не тронуты.
- Финал (оба кадра, Rust): sharp 0.92/1.28, resid 65/88 (17×/8×), LF 0.90/0.69, DC ~0. Края без рингинга, света и тёмные держат тон. В средних плашках мелкое зерно осталось — предел классики, дальше run4.
- Зеркало в Rust (`PrefilterCfg` + `blend_map`/`edge01_map`), 6 тестов зелёных.

**Артефакты:** `ml/preproc/{config,notch,edgenotch,blend,run_p0}.py`, `src-tauri/src/{prefilter.rs,app.rs}`; зональные замеры + кропы; задача 2.9 → DONE.

**Следующий шаг:** спека run4 (FFC@L0 + ResNet + DCT-входы + edge-loss + tone-loss + P1-данные) — классика упёрлась.

---

## 2026-09-14 — 2.8 DONE: цепочка в Rust, паритет с Python; три пойманных бага

**Контекст:** Перенос E1+bgswap+lintone в `run_descreen` (`src-tauri/src/prefilter.rs` + headless `--chain` в бенче).

**Решение/Результат:**
- Портированы: A0-замер (log+top-hat+симметрия), harmonic family, edge-адаптивный notch (Hann overlap-add), LF-детект/вычитание, bgswap (σ=32), linear-tone; blend-якорь — префильтрованный вход. Новых зависимостей нет (rustfft уже был).
- Тесты: 6 зелёных (FFT-roundtrip, DC-preservation, grating-notch, bgswap/tone stats, stage5 e2e, **chain-beats-raw на синтетическом AM** ÷13 на полосе).
- Паритет на реале (кадр 1): **Rust 0.943/30.0/0.942 vs Python 0.94/29.9/0.94** (sharp/resid/LF). Остаточная дельта mean|d|≈0.02–0.05 — версии ORT (rc.13 vs 1.24.4) + f32/f64, вне кода.
- Три бага, пойманных паритетом: (1) радиус notch в Rust был ×4 шире (Python неявно делил на 1024 через `measure_roi`) → введён явный `radius_ref_roi=1024` с обеих сторон; (2) Rust брал min масок вместо произведения (v3.7 — произведение); (3) **unshift на нечётных размерах** (холст LF 4283): двойной fftshift ≠ identity → поломанный выход, резкость падала до 0.086; теперь numpy-точные fftshift/ifftshift. (Точный механизм деградации, кроме сдвига, не установлен — но метрики сошлись полностью.)
- Диагностика стадий за env-флагом `DESCREEN_STAGE_DBG` (пики, bandE, дамп pre) — оставлена.
- Бинарь приложения пересобран (16.96 МБ): кнопка даёт новую цепочку. Headless-проверка: `descreen_bench --chain <file> --blend 1.0`.

**Артефакты:** `src-tauri/src/{prefilter.rs,app.rs,lib.rs,bin/descreen_bench.rs}`; задача 2.8 → DONE.

**Следующий шаг:** спека run4 (FFC@L0 + ResNet + DCT-входы + edge-loss + tone-loss + данные P1).

---

## 2026-09-14 — 2.7 DONE: фон бумаги возвращаем, тон на диете; ICC-план для P1

**Контекст:** Замечания: фон бумаги убивать нельзя; на зуме бороды — осветление и мыло; RGB→CMYK наивно конвертировать нельзя — нужны ICC по контексту.

**Решение/Результат:**
- `ml/preproc/paperbg.py`: split bg/detail (FFT-Gaussian σ=32, cutoff r≈0.005 — НЧ-полосы остаются в detail-пути), `return_background(src, net) = bg(src)+detail(net)` — фон возвращается побитово, сеть владеет только деталями. `linear_tone` (mean/std поканально) вместо CDF-стретча. Стадии `bgswap`/`lintone` в `run_p0`, всё мгновенно (без инференса, на сохранённых выходах).
- Матрица (кадр 1): **bgswap+lint — resid 29.9 (37× от 1125), sharp 0.94, DC ~0, LF 0.94**; CDF подтверждён вредным (47.7, перерастяжка); blend0.85+lint — resid 27.2/sharp 0.99/LF 1.12 (возвращает полосы). Зум бороды: точек нет, тон тёмный, выбеливания нет (`_e1/beard_bgswap.png`).
- ICC-почва: Pillow ImageCms на месте; системные ICC — струйные/холсты, офсетных нет. P1-план: скачать FOGRA52 (мелованный офсет → пресет magazine/fine), газетный (→ newspaper), uncoated (→ fine/упаковка?) с eci.org; RGB→CMYK через ImageCms с perceptual intent + TVI из пресетов; mapping пресет→профиль — в конфиг симулятора.

**Артефакты:** `ml/preproc/paperbg.py`, стадии в `run_p0.py`; `Data/RealScan/_e1/*bgswap*.png` + beard-кропы (gitignored); задача 2.7 → DONE.

**Следующий шаг:** вечерние фото → P1 (ICC-конверсия, FM, тон бумаги, НЧ-полосы в симулятор); спека run4; перенос E1+bgswap+lintone в Rust-бэкенд — по решению.

---

## 2026-09-14 — E1 DONE: edge-адаптивный notch убрал остатки на границах

**Контекст:** Задача 2.6. Остаточный растр концентрировался на контрастных переходах (край/плоскость 2.11, corr с градиентом 0.48): AM-экран × контент = боковые полосы, которые фиксированный notch не берёт.

**Решение/Результат:**
- `ml/preproc/edgenotch.py` + `EdgeNotchConfig` (патч 256/страйд 128, базовый радиус 6px@256, `edge_gain=2.0`, квантили контраста 0.5/0.95): карта локального контраста (средний градиент на патч) → радиус notch `base*(1+gain*c01)` → патч-wise Butterworth + Hann overlap-add. Пики замеряются глобально (A0), адаптируется только радиус.
- Кадр 1, стадия префильтра: край/плоскость **2.11 → 0.79**, corr с контрастом **+0.48 → −0.06**, абсолютная энергия на краях ÷4.2 (6.57e5→1.58e5); глобально resid 52, sharp 0.30.
- Полная E1-цепочка (anotch+LF+run3+tone): resid **57.0** (было 70.7), sharp 1.18, DC ~0.001. Визуально финал чистый, тон на месте.
- Попутный фикс: сломанная строка в `config.py` (слипшийся docstring LFConfig).

**Артефакты:** `ml/preproc/{edgenotch.py,config.py,run_p0.py}` (стадия `anotch`); выходы `Data/RealScan/_e1/` (gitignored); задача 2.6 → DONE.

**Следующий шаг:** спецификация run4 (FFC@L0 + ResNet + DCT-входы + edge-loss + данные: FM, тон бумаги, НЧ-полосы) — P2-бэклог; вечерние телефонные фото для калибровки.

---

## 2026-09-14 — P0 DONE: обёртка вокруг run3; атрибуция файлов выяснена по меткам

**Контекст:** Задача 2.5. P0 (A0-замер → notch → LF → run3 → тон) на паре RealScan (книжная иллюстрация 800 DPI, растр ~119 LPI).

**Атрибуция файлов (проверено метками NTFS + кодом):**
- `*_DS.png`: CreationTime и LastWriteTime — **09-07 вечером**. Наше Rust-приложение так не называет выходы: `app.rs:188` сохраняет `{stem}_descreened.png` (таких файлов в репо **нет вообще**). Конвенция `_DS.png` задокументирована за старым v3.7 (`Программа подавления растра и муара.md:1268`, `README.md:54`). Вывод: `*_DS.png` — выход **v3.7 от 09-07**, не нашей сети.
- Исходные `.tif` положены сегодня в 10:49, их mtime 2013/2023 — старые архивные сканы.
- Замеры «DC −0.024, резкость ÷7–100, НЧ ×2.2» из план-режима описывали **v3.7**, не run3. Честные цифры всех трёх (метрика `NoGTReport`, кадр 1; в скобках кадр 2):

| | Остат. растр xmean (исх. 1125/725) | Sharp-ratio | DC | LF-ratio |
|---|---|---|---|---|
| v3.7 (DS.png) | 74.8 | **0.008** (детали мертвы) | −0.024 | 0.87 |
| run3 raw (blend 1.0, DML) | 518 (снят лишь ~2×) | 2.09 (ВЧ-зерно) | **+0.10 (светлит — жалоба подтверждена)** | 0.69 |
| **P0: notch+LF+run3+tone** | **70.7 / 80** | 1.28 / 2.42 | ~0.000 | 1.03 / 0.94 |

**Решение/Результат:**
- Пакет `ml/preproc/` (`config/spectrum/notch/lfband/tone/metrics/run_p0`, конфиги-датаклассы, без магических констант): A0 — log+top-hat+порог+симметрия (дефолты v3.7: sens 2.5, order 3), замер r=0.1492@30.3° conf 0.54; notch-список = фундаментал + сокэкраны >0.4× + предсказанное гармоническое семейство (2r, r√2 ±45°); радиус notch 6px@256 (уже v3.7-дефолта 12).
- Два бага по ходу: (1) LF-детектор на top-hat слеп к широким НЧ-горбам → детект по сырому logmag + кластеризация голосов с допуском 0.0025; итог — полоса (0.0081,'v') поймана, LF 1.31→0.92; (2) метрика LF_ratio нормировалась на полную энергию и врала после notch → абсолютная энергия.
- Поведение run3 на реале (важно для P1/P2): светлит +0.085..0.10, поднимает ВЧ (×1.5–2.4 — частично зерно), давит НЧ-полосы ×0.6–0.7, растр снимает лишь наполовину. Тон-кривая: середина вверх до +0.19 локально — гистограммное согласование чинит маргиналы, но условное вымывание остаётся (аргумент за тон-разнообразие в симуляторе).
- Визуально оба P0-финала чистые, тон на месте; в кадре 2 есть колор-таргет — пригодится для валидации тона.
- Подтверждение 14.09 позже тем же днём: пользователь доложил правильные выходы приложения (`*_descreened.png`); их метрика — 465.7@0.149 / sharp 1.88 / DC +0.10 (кадр 1) и 215.7@0.148 / sharp 1.57 / DC +0.10 (кадр 2). Совпадение с прототипом бит-в-бит (465.67 vs 465.8 — разница лишь в округлении PNG): `ml/preproc` — точный прокси продакшен-пайплайна, выводы выше в силе.

**Артефакты:** `ml/preproc/*.py`; выходы `Data/RealScan/_p0/` + `_p0raw/` (gitignored); задача 2.5 → DONE.

**Следующий шаг:** вечерние телефонные фото (офсет/флексо/цифра) → калибровка симулятора P1 (тон бумаги, НЧ-полосы, FM-долг); доводка P0 (blend<1, мягкое тонирование); A3 палитра-маска и ResNet-P2 — после P1.

---

## 2026-09-14 — Этап 5 DONE: интерфейс работает; прод-сборка только через `tauri build`

**Контекст:** Окно приложения показывало «localhost refused to connect» вместо UI («интерфейс не работает»).

**Решение/Результат:**
- **Корень:** tauri-build выставляет `cfg(dev)` (через `cargo:dev` из build.rs крейта `tauri`), пока у зависимости `tauri` не включена фича `custom-protocol`; при `cfg(dev)` рантайм `get_app_url()` отдаёт `build.devUrl` вместо встроенного `frontendDist`. Обычный `cargo build` даёт dev-бинарник, требующий запущенного vite. Фича включается автоматически только через Tauri CLI (в бинарнике CLI есть `tauri/custom-protocol`).
- **Флоу:** прод — `tauri build --no-bundle --ci` (NSIS/WiX на машине нет, бандлинг — Этап 6); dev — `tauri dev` (vite :1420 + HMR). Для CLI нужен cargo в PATH (`$env:USERPROFILE\.cargo\bin`).
- **Попутные фиксы:** `default-run = "descreen-studio"` в Cargo.toml (два бинаря — CLI не находил main binary); Rust-крейт `tauri-plugin-dialog` + `.plugin(...)` (иначе `dialog:allow-open not found`); API-правки: `tauri::ipc::Invoke`, `resource_dir()`, `thumbnail()` без фильтра, `enumerate_pixels_mut()`; иконки через `tauri icon icon-src.png` (PIL-исходник).
- **Верификация:** `cargo test stage5_pipeline_end_to_end` ok (preview/spectrum/descreen_file на 256² CPU ~2 с); `vite build` ok (113 модулей); `tauri build --no-bundle` ok → `target/release/descreen-studio.exe` (16.9 МБ). Окно рендерит Svelte UI (тёмная тема, viewport, сайдбар спектра/параметров), нативный диалог открытия с фильтром «Сканы» работает (подтверждено скриншотом).
- **Отклонения от плана §7.1 (скетч):** команда — `run_descreen` (не `run_fourier_descreen`); превью — base64 data URL вместо asset-протокола (без scope-проблем); прогресс — событие `descreen-progress` {done,total}; движок лениво создаётся при первом запуске (DML с fallback на CPU).

**Артефакты:** `src/` (Svelte 5 + Tailwind v4, `src/dist/` в gitignore); `src-tauri/{tauri.conf.json, capabilities/default.json, icons/, build.rs, src/{app,runtime,lib,main}.rs, src/bin/descreen_bench.rs}`; `target/release/descreen-studio.exe` (локально, не в git).

**Следующий шаг:** ручная E2E-проверка на реальном скане (~2 мин/А4 на текущем железе); далее Этап 6 (NSIS/WiX, CI, model-артефакт в пайплайне) и фоновая 2.4.

---

## 2026-09-14 — Пересмотр критерия 4.1: замеренный профиль вместо <2.5 с

**Контекст:** Критерий 4.1 / §9.2 плана (А4@300DPI < 2.5 с на дискретной GPU) недостижим на текущем стеке: DirectML EP не имеет FFT-ядер, все DFT-узлы Fourier-UNet исполняются на CPU. Замерено на машине разработки (JOURNAL 2026-09-14, Этап 4): DML 125.2 с / CPU 223.7 с на скан А4@300DPI (48 тайлов 512²; DML ≈2.6 с/тайл, CPU ≈4.7 с/тайл; ускорение DML только на свёртках, ×1.8). Решение пользователя: критерий пересмотреть, идти в Этап 5.

**Решение/Результат:** Критерий приёмки пересмотрен — вместо абсолютного порога фиксируется замеренный эталонный профиль как baseline производительности (DML ≈125 с/А4, ≈2.6 с/тайл 512²; CPU ≈224 с/А4, ≈4.7 с/тайл). Целевой порог <2.5 с сохраняется как цель оптимизации в бэклоге (условия: CUDA12/cuFFT EP, TensorRT auto-FP16, batch-инференс тайлов, больший тайл) и не блокирует Этапы 5–6. Задача 4.1 закрыта по пересмотренному критерию; Этап 5 разблокирован.

**Артефакты:** `ImplementationPlan.md` §9 п.2, §8 (Этап 4); `TASKS.md` 4.1 → DONE, бэклог оптимизации.

**Следующий шаг:** Этап 5 — Tauri shell + Svelte 5 UI (viewport, FFT-спектр, split-сравнение), задача 5.1 → IN PROGRESS.

---

## 2026-09-14 — Этап 4: Rust-бенч (ort/DML) работает; тилинг 4.2 закрыт; критерий 4.1 не достигнут

**Контекст:** Задачи 4.1/4.2. Сборка Rust-бэкенда инференса и бенч-харнесса.

**Решение/Результат:**
- **Тулчейн:** Rust 1.98.1 + MSVC + Windows SDK 26100; `scripts/enter-vsenv.ps1` (импорт vcvars4.4.1), `scripts/fetch_ort_dll.ps1` (onnxruntime.dll + DirectML.dll из python-пакета onnxruntime-directml → `src-tauri/deps/`). `.gitignore`: target/, deps/, model*.onnx.
- **Скаффолд:** `src-tauri/Cargo.toml` (ort 2.0.0-rc.13: load-dynamic+directml+ndarray; **ndarray = "0.17"** — обязано совпадать с ort, 0.16 ломает типы; bin `descreen_bench`), `src-tauri/src/{lib.rs,inference.rs,main.rs}`.
- **orte API (rc.13 расходится со старыми доками):** `ort::init().commit()` (bool, не `.build()`); `Session::builder()?` без env; `ep::DirectML::default().build()` → `ExecutionProviderDispatch`; тензоры `ort::value::Tensor::from_array`; извлечение `outputs[0].try_extract_array::<f32>()` (а `try_extract_tensor` в rc.13 отдаёт `(&Shape, &[f32])`, не ndarray); `Session::get_providers()` отсутствует; `Session::run` требует `&mut self`.
- **descreen** (tile 512, stride 448, overlap 64): replicate-паддинг сетки тайлов на края кадра, Hann-окна, накопление acc/weight, нормализация на сумму весов, обрезка к [h,w]. `s!` — индекс `0` схлопывает ось батча (ранг 3), нужен `s![0..1, ...]`.
- **Баг найден и исправлен:** в `accumulate` прибавление веса окна стояло внутри цикла по каналам → вес ×3, результат темнел /3 (tiled vs full rmse 0.477 при одиночном тайле 512). Диагностика: дамп патчей (патч бит-в-бит равен входу, NaN/Inf нет), вес max=2.9999 при одном тайле. После фикса: **интерьер rmse 0.003, max 0.022, ни одного пикселя >0.05; полный кадр vs тилинг эквивалентны** (макс. 1.0 — только в 8px-углах, где вес окна Ханна→0).
- **Паритет Rust↔Python подтверждён прочими проверками:** process_tile детерминирован, срез-клон входа даёт тот же выход; модель size-инвариантна (512/768/1024 — mean(out−inp)=+0.08).
- **Латентность А4@300DPI (3508×2480, 48 тайлов 512²):** DML **125.2 с** (2.61 с/тайл, seam-sample 0.001), CPU **223.7 с** (4.66 с/тайл = Python CPU). **Причина невыполнения 4.1:** DirectML EP не имеет FFT/DFT-ядер → все DFT-узлы графа исполняются на CPU (fallback); DML ускоряет только свёртки (~×1.8). Полнокадровый DML-прогон 1400×1050 падает (Add «80070057» — ограничение DML на большие тензоры; некритично, тилинг — штатный путь).
- **Опции к 4.1:** (а) CUDA12/cuFFT — ort-gpu версии 1.24.4 API-уровня с cuDNN 9 + CUDA 12.8 (наш драйвер не тянет CUDA 13-колёса); (б) TensorRT EP auto-FP16 (нужна машина/облако с CUDA); (в) batch-инференс нескольких тайлов одним `session.run`; (г) пересмотр критерия на текущем железе (интегрированный GPU Win32, DFT всё равно на CPU). Решение — за пользователем.
- Follow-up: углы кадра (≈8 px) могут темнеть из-за веса окна→0; для сканов с полями некритично, но если понадобится — добавить внешний margin-overlap с обрезкой результата.

**Следующий шаг:** решение по 4.1; затем Tauri shell + Svelte (Этап 5) и задача 2.4 (baseline v3.7 vs UNet).

---

## 2026-09-14 — Этап 3 DONE: DFT-невалидность исправлена, GPU-путь через DirectML

**Контекст:** Задача 3.1/3.2. Экспорт в ONNX дал графы, не грузящиеся в строгих GPU EP.

**Решение/Результат:**
- **Корень бага:** torch.onnx эмитит `irfft2` как `DFT(inverse=1, onesided=1)` (5 узлов) — невалидно по ONNX-спецификации; CPU-ядро терпит, DML/CUDA/TensorRT отклоняют граф при загрузке.
- **Фикс в модели** (`ml/model/ffblock.py`): обратная инверсия теперь не onesided: rfft2 → поэлементные 1×1 conv → полный гермитов спектр в реальной области (conj с зеркалом по обеим осям, строка DC — сама себе пара) → `ifft2(norm="ortho")`. Математически тождественно обученному irfft2: паритет maxdiff ~2e-6 на 32×32…65×33, все веса чемпиона без переобучения.
- Флипы-зеркала пришлось заменить на `index_select` с позитивными индексами — `Slice` с шагом −1 не поддерживается DirectML (это и было причиной падения строгой загрузки после фикса DFT).
- Экспорт с both dynamo- и legacy-путями упирался в shape-guards/`remainder` с символьными делителями → выход: bounded `Dim("height"/"width", min=16, max=16384)` в `dynamic_shapes`.
- **Итоговые артефакты:** `fourier_descreen_unet.onnx` (исходный) + `fourier_descreen_unet.opt.onnx` (опт. 482→450 узлов, opt_level=99, экв. ~1e-7). Паритет на val: ΔPSNR **+0.001**, ΔSSIM 0.9261 vs 0.9261 (torch-cuda).
- **GPU-латентность (эталон для 4.1, А4@300DPI < 2.5 с):** CPU-пик катастрофичен — 512² 4.7 с / 768² 34 с; **DirectML EP**: 512² 2.6 с / 768² 6.4 с, maxdiff 2.6e-6 (одна сессия, DML+CPU-fallback). CUDA EP на этой машине недоступен (нужен CUDA 13: у нас драйвер 12.8; для py314 нет колёс ort<1.29; `nvidia-cublas-cu13` — только sdist). Строгий DML fail из-за shape-опов, роутимых в CPU намеренно — benign.
- Резюме на будущее (встраивание): DirectML — единственный рабочий GPU-провайдер здесь; FP16 — только через TensorRT EP (машины с CUDA 13+).

**Следующий шаг:** Этап 4 — Rust/Tauri бэкенд (`ort` binding + тилинг overlap-add Ханна из v3.7) и задача 2.4 (baseline v3.7 vs UNet на реальных сканах). Нужна установка Rust (rustc/cargo отсутствуют).

---

**Контекст:** run3 — потолок постановки (варианты: архитектура, вейвлет, blind-head, разделение каналов, сэмплер). Решение: закрываем Этап 2 на 32.88/0.9070 (до критерия 34/0.95 не дотянули — фиксируем честно, цель в фоновый R&D-бэклог), переходим к Этапу 3 + 2.4.

**Решение/Результат:**
- `ml/export_onnx.py`: экспорт champion (base96/L3/bneck3/spec(False,True,True) @ opset 18, динамические оси batch/height/width, weights embedded) → `ml/export/fourier_descreen_unet.onnx` (fp32, 41.8 МБ). ORT: **ort-cpu == ort-cuda бит-в-бит (0.0)**, max_abs_err vs torch-cpu 1.3e-6.
- `ml/export_validate.py`: на 8 val-парах ΔPSNR ort-fp32-cpu vs torch-cuda = **−0.19**, ΔSSIM = −0.0003. Паритет подтверждён; расхождение 0.2 max на шуме — нестабильность torch между CPU/CUDA (cufft vs pocketfft), а не экспорт.
- **FP16-файл закрыт**: полный конвертер (onnxconverter-common) ломает граф (Cast в комплексном DFT-пути — type error). FP16 в продакшене даёт TensorRT EP автоматически (auto-FP16). Артефакт: только fp32.
- Урок процесса: find пути в neokor-стиле — `parents[1]` (не [2]) для файла в `ml/`; консоль — `PYTHONIOENCODING=utf-8` (cp1252 ём на ✅).

**Следующий шаг:** задача 2.4 — сравнение классики v3.7/notch против ONNX на реальных сканах, затем Этап 3: план `src-tauri/` + `src/` (Rust/Svelte встраивание ort).

---

## 2026-09-13 — Wavelet-вердикт: Fourier-UNet побеждает, round-2 закрыт

**Контекст:** Проверка вейвлет-направления (MWCNN: DWT/IWT Хаара вместо stride-down/up) и blind-head (FBCNN-идея: aux-предсказание preset×DPI из манифеста).

**Решение/Результат:** `ml/model/wavelet.py` (MWNet 8.9M, точная реконструкция DWT/IWT 1e-7), `ml/model/blind.py`, `ml/train/search_wavelet.py`. Прокси-итоги (3 эпохи, 2k патчей):
- fourier-чемпион: 26.34 (воспроизведён точь-в-точь)
- wavelet чистый: 25.30 (bneck2) / 25.70 (bneck3) — стабильно на 0.6–1.0 dB хуже
- blind-head вредит везде: fourier+blind 25.05→pruned, wavelet+blind pruned на эп.0 (22.4–22.5)
- Вывод: spectral-ветвь FFC уже покрывает частотную селективность; DWT-даунсэмпл прироста не даёт, aux-задача отвлекает на коротком горизонте. Вейвлет-путь закрыт, модель финала — fourier base96/bneck3/spec-off/f0.05/lpips0 (уже обучена в run2: 32.61/0.905).
- Процессное: длинные прогоны — только с записью в лог-файл (`*>> log`), чанками под таймаут; `study.tell(n, state=...)` только с keyword.

**Следующий шаг:** решение — run3 (дожим к 34) или Этап 3 (ONNX) + 2.4 (baseline v3.7).

---

## 2026-09-13 — E1b ПРОВАЛ: CMYK-notch хуже RGB, трек каналов закрыт

**Контекст:** Проверка находки E0 (K-канал 10.2 dB) делом: поканальный notch в CMYK против RGB-notch.

**Решение/Результат:** `ml/analysis/e1b_notch.py`, 20 парных сканов. RGB-notch давит остатки на −14.4 dB, CMYK-notch — только −4.9 dB. PSNR обоих ~16.0 против 15.78 у исходного скана (растр мал по MSE, notch режет контент сопоставимо). Причина: изоляция была широкополосной, а notch нужны острые пики; наивное деление их размазывает. Ошибка моя: перепутал broadband- и spectral-критерии. Трек разделения каналов закрыт полностью.

**Следующий шаг:** Этап 2 закрываем на run3 (32.88/0.907, до 34 не дотянули — фиксируем честно) → Этап 3 (ONNX) + 2.4 (baseline v3.7).

---

## 2026-09-13 — E0 DONE: разделение каналов не дотянуло до E1

**Контекст:** Проверка идеи монохромных печатных каналов (PCA/NMF на optical density, изоляция по истинным пластинам из симулятора).

**Решение/Результат:** `ml/analysis/dump_plates.py` (20 сканов + 80 истинных масок), `ml/analysis/e0_separation.py`, отчёт `knowledge/channel-separation.md`. Изоляция (среднее): RGB 4.01 / naive-CMYK 6.74 / PCA 7.64 / NMF-3 5.25 dB. Критерий E1 (≥10) не выполнен. Попутно: NMF k=4 невозможен на RGB (rank-3), per-image NMF нестабилен (0.053). Находка: наивный K-канал изолируется на 10.16 dB — открыт дешёвый трек E1b (классический CMYK-notch без обучения).

**Следующий шаг:** E1b-проба или закрытие трека (на решение пользователя).

---

## 2026-09-13 — Run2: 32.61 / 0.905, поиск завершён чемпионом

**Контекст:** Этап 2. Run1 дал 30.84/0.887 (цели 34/0.95). Optuna-lite поиск (прокси: 2k патчей, 3 эпохи) + полное обучение чемпиона.

**Решение/Результат:**
- Поиск (sqlite `ml/train/search.db`, ~25 trials): лидер — base96/L3/bottleneck3, spectral на полном разрешении ВЫКЛ, fourier_w 0.05, lpips_w 0.0 (прокси 26.34). Сигналы: capacity важнее всего (96>64>48); LPIPS душит PSNR (во всех топах 0); spectral-ветвь на полном разрешении избыточна.
- Run2 (12 эпох, batch 4, AMP, EMA-оценка, champion arch 10.4M): **val 32.61 / 0.905** (эп.11). Прогресс +1.77 dB к run1. До целей не дотянули (~1.4 dB), кривая затухает.
- Инфраструктура: EMA (decay 0.999) в `train.py`, пропуск LPIPS-forward при w=0, legacy-загрузчик чекпоинтов при смене имён слоёв, `diagnose.py` (ранжирование + тройки scan/pred/gt худших).
- Урок-инцидент: `study.tell(n, TrialState.FAIL)` ставит values=3 (FAIL==3 в enum), а не state — убил 4 trials оценкой 3.0. Правильно: `tell(n, state=...)`. Плюс: фоновые процессы глохнут — длинные прогоны только foreground с resume.
- Диагностика хвоста: худшие патчи — тёмные текстуры с муаром (остаточная пятнистость), мелкий спекл (сглаживание текстур), тонкие белые линии (модель стирает как пыль).

**Следующий шаг:** частотно-вейвлетное направление — Frequency-domain NAS / Wavelet-NAS, Fourier Blind CNN, Multi-Level Wavelet CNN (MWCNN) как кандидатная архитектура.

---

## 2026-09-12 — Задачи 2.1/2.2 DONE: Fourier-UNet + composite loss

**Контекст:** Этап 2, модель и лосс по плану §4.

**Решение/Результат:** `ml/model/` — `ffblock.py` (FourierSpectralBlock по плану + FFCBlock со spatial-ветвью, fusion и residual), `unet.py` (настоящий энкодер-декодер 64→128→256 с FFC-блоками на всех уровнях + скипы, residual-выход; план §4 давал игрушечный одноблочный вариант — расширен до рабочей архитектуры), `losses.py` (LossConfig 1.0/0.5/0.1/0.1; LPIPS-Alex frozen). Параметров 3.61M — в бюджет 70 МБ сборки с запасом (~14 МБ FP32, ~7 МБ FP16).
- Smoke CUDA: forward 2×512 0.57 с, loss+backward ~1 с, пик VRAM 6 ГБ → рабочий батч 4×512.
- Оверфит-тест на реальном патче: 0.75→0.40 за 20 шагов — градиенты текут, сеть учится убирать растр.

**Следующий шаг:** 2.3 — скрипт обучения (AMP, чекпоинты, val PSNR/SSIM) и полный прогон.

---

## 2026-09-12 — GPU вопрос решён: обучаемся локально

**Контекст:** torch был CPU-only, обучение Этапа 2 было заблокировано.

**Решение/Результат:** В машине RTX 3080 Ti Laptop 16 ГБ (драйвер 595.79 / CUDA 13.2) + Iris Xe. Поставлен torch 2.11.0+cu128 (пришлось через --force-reinstall: pip считал +cpu «тем же» 2.13.0). Smoke: forward+conv+rFFT на батче 4×512 за 0.23 с, 0.8 ГБ VRAM. Облако не нужно — обучаемся локально, AMP, батч 4–8×512 влезет с запасом. Конфликт numba/numpy 2.5.2 безвреден (numba нашим кодом не используется).

**Следующий шаг:** Этап 2 — Fourier-UNet (2.1/2.2), затем обучение 2.3.

---

## 2026-09-08 — Задача 1.6 DONE: датасет 10k/1k, Этап 1 закрыт

**Контекст:** Финальная задача Этапа 1.

**Решение/Результат:** `ml/dataset/generate.py` — детерминированный план (seed 11), пул процессов, resume через пропуск готового. `Data/synth/train` 10 000 пар + `Data/synth/val` 1 000 пар (512×512 PNG, 8.8 ГБ), манифесты с полными параметрами каждого патча (источник, кроп, preset, LPI, DPI, сиды, параметры сканера, gt_top_peak). Распределения по плану: magazine ~50%, 300 DPI ~60%, фото ~65% (из них ~20% с заголовками), документы ~35%. Сплит по исходникам, утечек train/val нет, битых файлов 0. Время: ~5 ч на 12 воркерах.
- По ходу найден и исправлен баг композита из 1.3: мультипликативный композит × тонирование давал двойное затемнение (скан 0.15 при GT 0.28). Заменено на поканальную субтрактивную модель R=paper·(1−C)(1−K) и т.д. Тонопередача стала sane (фото 0.28→0.21 с TVI, документ 0.90→0.84). Геометрия растра и все спектральные выводы 1.3–1.4 не затронуты.
- Превью `_preview*/` от 1.3 сделаны старым композитом (темнее); геометрия/спектры валидны, перегенерировать не требуется.

**Этап 1 полностью DONE.** Следующий — Этап 2: Fourier-UNet + обучение.

---

## 2026-09-08 — Задача 1.5 DONE: в DESCAN-сканax растра нет

**Контекст:** Первое касание архивов (Valid/Test, чтение в память; Train.zip не тронут).

**Решение/Результат:** `ml/simulator/analyze_refs.py`, 80 сканов, сырые данные `ml/simulator/_refs/refs.json`, отчёт `knowledge/reference-spectra.md`. Топ-пики всех сканов — контент вёрстки (r 0.01–0.05, xmean до 800); растровых пиков у Найквиста 0/80; единственные ВЧ-пики (7/80) — строго ±90°, линейки текста. HF-пол: Valid 0.07, Test 0.15. Дефолты симулятора не меняем (физика > эти сканы). Решение об исключении DESCAN-18K получило прямое спектральное подтверждение.

**Следующий шаг:** задача 1.6 — генератор 10k/1k пар.

---

## 2026-09-08 — Задача 1.2 DONE: 500 страниц документов

**Контекст:** GT документов с мелким кеглем — нет ни в одном готовом датасете, рендерим сами.

**Решение/Результат:** `ml/dataset/documents.py` — 16 текстов Gutenberg (`Data/gt/doc_texts/`), вёрстка A4 @ 600 DPI (4962×7014) в `Data/gt/documents/` + `manifest.jsonl` (книга, шрифт, кегль, колонки, seed 3). Шрифты: Times/Georgia/Arial/Verdana/Calibri/Courier/DejaVu (serif/sans/mono); тело 9–12pt, заголовки 16–24pt, fine print 4–6pt на 477/500 стр.; таблицы и фигуры-иллюстрации, 1–2 колонки, номера страниц. Объём 0.68 ГБ, покрытие краской 4–26% (текст на месте, разброс по вёрстке — нормально).

**Следующий шаг:** задача 1.5 (анализ DESCAN-архивов — первое касание) → 1.6 (генератор 10k/1k).

---

## 2026-09-08 — Задача 1.1 DONE: 2000 фото Unsplash

**Контекст:** Полный объём GT-фото для 1.6 (11k патчей, ~70% фото / ~30% документы из 1.2).

**Решение/Результат:** `Data/gt/unsplash/` — 2000 фото (w=2560, q=95), битых 0/2000, `manifest.jsonl` (photo_id, URL, размеры; seed 7 — выбор воспроизводим). Спот-чек периодики на 10 фото: пики до 300× среднего — это контентная периодика (фасады/текстуры), допустима; систематическая отбраковка «грязного» GT — в задаче 1.6. DIV2K решено не качать: 2000 фото × ~10–15 патчей ≈ 25k кандидатов, объёма хватает с запасом.

**Следующий шаг:** задача 1.2 — рендер документов.

---

## 2026-09-08 — Шаг 2 готов: каскад «Сканер», муар рождается алиасингом (задача 1.4 DONE)

**Контекст:** Этап 1, задача 1.4. Вход — оттиск из 1.3, выход — скан 300/600 DPI.

**Решение/Результат:** `ml/simulator/config.py` (ScanConfig: поворот 0.1–2°, PSF σ 0.3–1.2, Гаусс 0.004–0.012, Пуассон пик 40–120, пыль), `ml/simulator/scan.py` (`sample_scan_params`, `rotate_rgb`, `_add_dust`, `simulate_scan`), `ml/simulator/preview_scan.py`, мишень `flat` в `targets.py`.
- Порядок физически корректный: поворот → PSF в печатном пространстве (σ×S) → BOX-даунсэмпл → шумы → пыль.
- Доказательство муара на плоском поле (magazine 133 LPI, скан 300 DPI): lowband-энергия 0.051 при 0° → 0.072 при 0.5° → **0.194 при 1.5° (×3.8)**; пики растра на 0.443 цикл/px (теория 133/2400×8 = 0.443 ✓). Муар рисуется только ресемплингом, постфактум — ничего.
- На фото lowband-метрика тонет в контенте (ожидаемо) — для контента контроль визуальный: `ml/simulator/_scan/`.
- Важно для 1.6: генератор пар обязан поворачивать GT тем же углом (`rotate_rgb`), иначе пары разъедутся.

**Артефакты:** `ml/simulator/scan.py`, `preview_scan.py`, `ml/simulator/_scan/`.

**Следующий шаг:** задача 1.1 (докачка Unsplash до полного объёма) + 1.2 (рендер документов) → 1.6 (генератор 10k/1k).

---

## 2026-09-08 — Шаг 1 готов: каскад «Печать» AM (задача 1.3 DONE)

**Контекст:** Этап 1, задача 1.3. Только AM-растр, GT = Unsplash dev (20 фото, `Data/gt/dev/` + `manifest.jsonl`). Архивы DESCAN-18K не тронуты. Метаданные Unsplash (`photos.tsv000`) читаются из zip в память; из 25 000 строк отобрано 24 028 featured-фото с меньшей стороной ≥2000 px.

**Решение/Результат:** `ml/simulator/` — `config.py` (PrintConfig, пресеты newspaper 95 / magazine 133 / fine 165 LPI, углы C15/M75/Y0/K45 ±3°, TVI-гейн, `for_scan_dpi()`), `color.py` (RGB→CMYK), `screen.py` (ячейка, поворот, круг/эллипс/квадрат, построчная обработка чанками), `compose.py` (композит, оптический blur, бумага), `targets.py` (процедурные мишени), `preview.py` (верификация + бенчмарк).
- Пики 2D FFT синтетики на расчётных частотах: newspaper/fine/magazine, ошибка <1% (напр. 0.0552 против теории 0.0554 цикл/px; доминирует K-канал 45°).
- Тонопередача монотонна 0→0.97, полный диапазон, видимый dot gain.
- Бенчмарк CPU (патч 512): x4 ≈7 с, x8 ≈29 с. `oversample = print_dpi / scan_dpi` (x8 для 300 DPI, x4 для 600 DPI); печатное пространство 2400 DPI eq. — ячейка 14–28 px, достаточно для форм точек. 4800 DPI отложено (×4 цена без необходимости).
- Превью (scan-view, кропы 100%, спектры): `ml/simulator/_preview/`, `ml/simulator/_preview2/`.

**Артефакты:** `ml/simulator/*.py`, `ml/dataset/unsplash.py`, `Data/gt/dev/` (20 фото).

**Следующий шаг:** задача 1.4 — каскад «Сканер» (поворот, PSF, ресемплинг, шумы; муар — алиасингом).

---

## 2026-09-08 — Стратегия данных проекта (решение)

**Контекст:** Выбор источников обучающих данных для нейросетевого дескрининга. В проекте лежит DESCAN-18K (18 360 реальных пар скан/оригинал).

**Решение/Результат:**
1. **DESCAN-18K исключён из обучения.** Причины: (а) сканы с офисных МФУ (Canon imageRUNNER, Plustek OpticBook) не соответствуют продакшн-входу (планшетные/барабанные сканеры печатной продукции с выраженным растром и сканерным муаром); (б) GT выровнен AKAZE — не пиксельно-точный, потолок SOTA на нём ~23 dB PSNR; (в) лицензия CC BY-NC-SA — некоммерческая, а статус продукта не определён → закладываем коммерчески чистое ядро.
2. **Обучающее ядро — синтетика:** Unsplash Lite + DIV2K (фото) + собственный рендер документов (текст от 4 pt, 600 DPI; готовых датасетов с таким GT нет).
3. **Симулятор — двухкаскадный:** «виртуальная печать» в эквиваленте 2400–4800 DPI → «виртуальный сканер» (поворот 0.1–2°, PSF, ресемплинг до 300/600 DPI). Муар должен возникать как алиасинг при ресемплинге, а не накладываться постфактум. Критический режим: 150 LPI @ 300 DPI → период растра 2 px (граница Найквиста).
4. **DESCAN-18K — только референс дефектов:** измерить спектральные портреты сканов из Valid для калибровки симулятора (задача 1.5). Train.zip (35 ГБ) не распаковывать.
5. **DSP v3.7 (текущий браузерный продукт)** — тройная роль: baseline-планка качества; детектор остаточных периодических пиков (σ-thresholding в 2D FFT) как метрика на реальных сканах без GT; источник отлаженных алгоритмов (tiling + Hann overlap-add, FFT-визуализация, пресеты типов печати) для портирования в Rust/фронтенд.
6. **Метрики:** PSNR > 34 dB / SSIM > 0.95 — только на отложенной синтетике. На реальных сканах — энергия остаточных пиков спектра + качественная оценка vs v3.7.

**Артефакты:** `AGENTS.md`, `WORKFLOW.md`, `TASKS.md`, `JOURNAL.md`, `knowledge/`.

**Следующий шаг:** Этап 1 — задача 1.3/1.4 (симулятор «печать → сканер»).
