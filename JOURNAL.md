# Journal

Хронология решений, экспериментов и прогресса. Формат записи — см. `WORKFLOW.md`. Новые записи добавляются **сверху**.

---

## 2026-09-14 — Этап 3 DONE: DFT-невалидность исправлена, GPU-путь через DirectML

**Контекст:** Задача 3.1/3.2. Экспорт в ONNX дал графы, не грузящиеся в строгих GPU EP.

**Решение/Результат:**
- **Корень бага:** torch.onnx эмитит `irfft2` как `DFT(inverse=1, onesided=1)` (5 узлов) — невалидно по ONNX-спецификации; CPU-ядро терпит, DML/CUDA/TensorRT отклоняют граф при загрузке.
- **Фикс в модели** (`ml/model/ffblock.py`): обратная инверсия теперь не onesided: rfft2 → поэлементные 1×1 conv → полный гермитов спектр в реальной области (conj с зеркалом по обеим осям, строка DC — сама себе пара) → `ifft2(norm="ortho")`. Математически тождественно обученному irfft2: паритет maxdiff ~2e-6 на 32×32…65×33, все веса чемпиона без переобучения.
- Флипы-зеркала пришлось заменить на `index_select` с позитивными индексами — `Slice` с шагом −1 не поддерживается DirectML (это и было причиной падения строгой загрузки после фикса DFT).
- Экспорт с both dynamo- и legacy-путями упирался в shape-guards/`remainder` с символьными делителями → выход: bounded `Dim("height"/"width", min=16, max=16384)` в `dynamic_shapes`.
- **Итоговые артефакты:** `fourier_descreen_unet.onnx` (исходный) + `fourier_descreen_unet.opt.onnx` (опт. 482→450 узлов, opt_level=99, экв. ~1e-7). Паритет на val: ΔPSNR **+0.001**, ΔSSIM 0.9261 vs 0.9261 (torch-cuda).
- **GPU-латентность (эталон для 4.1, А4@300DPI < 2.5 с):** CPU-пик катастрофичен — 512² 4.7 с / 768² 34 с; **DirectML EP**: 512² 2.6 с / 768² 6.4 с, maxdiff 2.6e-6 (одна сессия, DML+CPU-fallback). CUDA EP на этой машине недоступен (нужен CUDA 13: у нас драйвер 12.8; для py314 нет колёс ort<1.29; `nvidia-cublas-cu13` — только sdist). Строгий DML fail из-за shape-опов, роутимых в CPU намеренно — benign.
- Резюме на будущее (встраивание): DirectML — единственный рабочий GPU-провайдер здесь; FP16 — только через TensorRT EP (машины с CUDA 13+).

**Следующий шаг:** Этап 4 — Rust/Tauri бэкенд (`ort` binding + тилинг overlap-add Ханна из v3.7) и задача 2.4 (baseline v3.7 vs UNet на реальных сканах). Нужна установка Rust (rustc/cargo отсутствуют).

---

**Контекст:** run3 — потолок постановки (варианты: архитектура, вейвлет, blind-head, разделение каналов, сэмплер). Решение: закрываем Этап 2 на 32.88/0.9070 (до критерия 34/0.95 не дотянули — фиксируем честно, цель в фоновый R&D-бэклог), переходим к Этапу 3 + 2.4.

**Решение/Результат:**
- `ml/export_onnx.py`: экспорт champion (base96/L3/bneck3/spec(False,True,True) @ opset 18, динамические оси batch/height/width, weights embedded) → `ml/export/fourier_descreen_unet.onnx` (fp32, 41.8 МБ). ORT: **ort-cpu == ort-cuda бит-в-бит (0.0)**, max_abs_err vs torch-cpu 1.3e-6.
- `ml/export_validate.py`: на 8 val-парах ΔPSNR ort-fp32-cpu vs torch-cuda = **−0.19**, ΔSSIM = −0.0003. Паритет подтверждён; расхождение 0.2 max на шуме — нестабильность torch между CPU/CUDA (cufft vs pocketfft), а не экспорт.
- **FP16-файл закрыт**: полный конвертер (onnxconverter-common) ломает граф (Cast в комплексном DFT-пути — type error). FP16 в продакшене даёт TensorRT EP автоматически (auto-FP16). Артефакт: только fp32.
- Урок процесса: find пути в neokor-стиле — `parents[1]` (не [2]) для файла в `ml/`; консоль — `PYTHONIOENCODING=utf-8` (cp1252 ём на ✅).

**Следующий шаг:** задача 2.4 — сравнение классики v3.7/notch против ONNX на реальных сканах, затем Этап 3: план `src-tauri/` + `src/` (Rust/Svelte встраивание ort).

---

## 2026-09-13 — Wavelet-вердикт: Fourier-UNet побеждает, round-2 закрыт

**Контекст:** Проверка вейвлет-направления (MWCNN: DWT/IWT Хаара вместо stride-down/up) и blind-head (FBCNN-идея: aux-предсказание preset×DPI из манифеста).

**Решение/Результат:** `ml/model/wavelet.py` (MWNet 8.9M, точная реконструкция DWT/IWT 1e-7), `ml/model/blind.py`, `ml/train/search_wavelet.py`. Прокси-итоги (3 эпохи, 2k патчей):
- fourier-чемпион: 26.34 (воспроизведён точь-в-точь)
- wavelet чистый: 25.30 (bneck2) / 25.70 (bneck3) — стабильно на 0.6–1.0 dB хуже
- blind-head вредит везде: fourier+blind 25.05→pruned, wavelet+blind pruned на эп.0 (22.4–22.5)
- Вывод: spectral-ветвь FFC уже покрывает частотную селективность; DWT-даунсэмпл прироста не даёт, aux-задача отвлекает на коротком горизонте. Вейвлет-путь закрыт, модель финала — fourier base96/bneck3/spec-off/f0.05/lpips0 (уже обучена в run2: 32.61/0.905).
- Процессное: длинные прогоны — только с записью в лог-файл (`*>> log`), чанками под таймаут; `study.tell(n, state=...)` только с keyword.

**Следующий шаг:** решение — run3 (дожим к 34) или Этап 3 (ONNX) + 2.4 (baseline v3.7).

---

## 2026-09-13 — E1b ПРОВАЛ: CMYK-notch хуже RGB, трек каналов закрыт

**Контекст:** Проверка находки E0 (K-канал 10.2 dB) делом: поканальный notch в CMYK против RGB-notch.

**Решение/Результат:** `ml/analysis/e1b_notch.py`, 20 парных сканов. RGB-notch давит остатки на −14.4 dB, CMYK-notch — только −4.9 dB. PSNR обоих ~16.0 против 15.78 у исходного скана (растр мал по MSE, notch режет контент сопоставимо). Причина: изоляция была широкополосной, а notch нужны острые пики; наивное деление их размазывает. Ошибка моя: перепутал broadband- и spectral-критерии. Трек разделения каналов закрыт полностью.

**Следующий шаг:** Этап 2 закрываем на run3 (32.88/0.907, до 34 не дотянули — фиксируем честно) → Этап 3 (ONNX) + 2.4 (baseline v3.7).

---

## 2026-09-13 — E0 DONE: разделение каналов не дотянуло до E1

**Контекст:** Проверка идеи монохромных печатных каналов (PCA/NMF на optical density, изоляция по истинным пластинам из симулятора).

**Решение/Результат:** `ml/analysis/dump_plates.py` (20 сканов + 80 истинных масок), `ml/analysis/e0_separation.py`, отчёт `knowledge/channel-separation.md`. Изоляция (среднее): RGB 4.01 / naive-CMYK 6.74 / PCA 7.64 / NMF-3 5.25 dB. Критерий E1 (≥10) не выполнен. Попутно: NMF k=4 невозможен на RGB (rank-3), per-image NMF нестабилен (0.053). Находка: наивный K-канал изолируется на 10.16 dB — открыт дешёвый трек E1b (классический CMYK-notch без обучения).

**Следующий шаг:** E1b-проба или закрытие трека (на решение пользователя).

---

## 2026-09-13 — Run2: 32.61 / 0.905, поиск завершён чемпионом

**Контекст:** Этап 2. Run1 дал 30.84/0.887 (цели 34/0.95). Optuna-lite поиск (прокси: 2k патчей, 3 эпохи) + полное обучение чемпиона.

**Решение/Результат:**
- Поиск (sqlite `ml/train/search.db`, ~25 trials): лидер — base96/L3/bottleneck3, spectral на полном разрешении ВЫКЛ, fourier_w 0.05, lpips_w 0.0 (прокси 26.34). Сигналы: capacity важнее всего (96>64>48); LPIPS душит PSNR (во всех топах 0); spectral-ветвь на полном разрешении избыточна.
- Run2 (12 эпох, batch 4, AMP, EMA-оценка, champion arch 10.4M): **val 32.61 / 0.905** (эп.11). Прогресс +1.77 dB к run1. До целей не дотянули (~1.4 dB), кривая затухает.
- Инфраструктура: EMA (decay 0.999) в `train.py`, пропуск LPIPS-forward при w=0, legacy-загрузчик чекпоинтов при смене имён слоёв, `diagnose.py` (ранжирование + тройки scan/pred/gt худших).
- Урок-инцидент: `study.tell(n, TrialState.FAIL)` ставит values=3 (FAIL==3 в enum), а не state — убил 4 trials оценкой 3.0. Правильно: `tell(n, state=...)`. Плюс: фоновые процессы глохнут — длинные прогоны только foreground с resume.
- Диагностика хвоста: худшие патчи — тёмные текстуры с муаром (остаточная пятнистость), мелкий спекл (сглаживание текстур), тонкие белые линии (модель стирает как пыль).

**Следующий шаг:** частотно-вейвлетное направление — Frequency-domain NAS / Wavelet-NAS, Fourier Blind CNN, Multi-Level Wavelet CNN (MWCNN) как кандидатная архитектура.

---

## 2026-09-12 — Задачи 2.1/2.2 DONE: Fourier-UNet + composite loss

**Контекст:** Этап 2, модель и лосс по плану §4.

**Решение/Результат:** `ml/model/` — `ffblock.py` (FourierSpectralBlock по плану + FFCBlock со spatial-ветвью, fusion и residual), `unet.py` (настоящий энкодер-декодер 64→128→256 с FFC-блоками на всех уровнях + скипы, residual-выход; план §4 давал игрушечный одноблочный вариант — расширен до рабочей архитектуры), `losses.py` (LossConfig 1.0/0.5/0.1/0.1; LPIPS-Alex frozen). Параметров 3.61M — в бюджет 70 МБ сборки с запасом (~14 МБ FP32, ~7 МБ FP16).
- Smoke CUDA: forward 2×512 0.57 с, loss+backward ~1 с, пик VRAM 6 ГБ → рабочий батч 4×512.
- Оверфит-тест на реальном патче: 0.75→0.40 за 20 шагов — градиенты текут, сеть учится убирать растр.

**Следующий шаг:** 2.3 — скрипт обучения (AMP, чекпоинты, val PSNR/SSIM) и полный прогон.

---

## 2026-09-12 — GPU вопрос решён: обучаемся локально

**Контекст:** torch был CPU-only, обучение Этапа 2 было заблокировано.

**Решение/Результат:** В машине RTX 3080 Ti Laptop 16 ГБ (драйвер 595.79 / CUDA 13.2) + Iris Xe. Поставлен torch 2.11.0+cu128 (пришлось через --force-reinstall: pip считал +cpu «тем же» 2.13.0). Smoke: forward+conv+rFFT на батче 4×512 за 0.23 с, 0.8 ГБ VRAM. Облако не нужно — обучаемся локально, AMP, батч 4–8×512 влезет с запасом. Конфликт numba/numpy 2.5.2 безвреден (numba нашим кодом не используется).

**Следующий шаг:** Этап 2 — Fourier-UNet (2.1/2.2), затем обучение 2.3.

---

## 2026-09-08 — Задача 1.6 DONE: датасет 10k/1k, Этап 1 закрыт

**Контекст:** Финальная задача Этапа 1.

**Решение/Результат:** `ml/dataset/generate.py` — детерминированный план (seed 11), пул процессов, resume через пропуск готового. `Data/synth/train` 10 000 пар + `Data/synth/val` 1 000 пар (512×512 PNG, 8.8 ГБ), манифесты с полными параметрами каждого патча (источник, кроп, preset, LPI, DPI, сиды, параметры сканера, gt_top_peak). Распределения по плану: magazine ~50%, 300 DPI ~60%, фото ~65% (из них ~20% с заголовками), документы ~35%. Сплит по исходникам, утечек train/val нет, битых файлов 0. Время: ~5 ч на 12 воркерах.
- По ходу найден и исправлен баг композита из 1.3: мультипликативный композит × тонирование давал двойное затемнение (скан 0.15 при GT 0.28). Заменено на поканальную субтрактивную модель R=paper·(1−C)(1−K) и т.д. Тонопередача стала sane (фото 0.28→0.21 с TVI, документ 0.90→0.84). Геометрия растра и все спектральные выводы 1.3–1.4 не затронуты.
- Превью `_preview*/` от 1.3 сделаны старым композитом (темнее); геометрия/спектры валидны, перегенерировать не требуется.

**Этап 1 полностью DONE.** Следующий — Этап 2: Fourier-UNet + обучение.

---

## 2026-09-08 — Задача 1.5 DONE: в DESCAN-сканax растра нет

**Контекст:** Первое касание архивов (Valid/Test, чтение в память; Train.zip не тронут).

**Решение/Результат:** `ml/simulator/analyze_refs.py`, 80 сканов, сырые данные `ml/simulator/_refs/refs.json`, отчёт `knowledge/reference-spectra.md`. Топ-пики всех сканов — контент вёрстки (r 0.01–0.05, xmean до 800); растровых пиков у Найквиста 0/80; единственные ВЧ-пики (7/80) — строго ±90°, линейки текста. HF-пол: Valid 0.07, Test 0.15. Дефолты симулятора не меняем (физика > эти сканы). Решение об исключении DESCAN-18K получило прямое спектральное подтверждение.

**Следующий шаг:** задача 1.6 — генератор 10k/1k пар.

---

## 2026-09-08 — Задача 1.2 DONE: 500 страниц документов

**Контекст:** GT документов с мелким кеглем — нет ни в одном готовом датасете, рендерим сами.

**Решение/Результат:** `ml/dataset/documents.py` — 16 текстов Gutenberg (`Data/gt/doc_texts/`), вёрстка A4 @ 600 DPI (4962×7014) в `Data/gt/documents/` + `manifest.jsonl` (книга, шрифт, кегль, колонки, seed 3). Шрифты: Times/Georgia/Arial/Verdana/Calibri/Courier/DejaVu (serif/sans/mono); тело 9–12pt, заголовки 16–24pt, fine print 4–6pt на 477/500 стр.; таблицы и фигуры-иллюстрации, 1–2 колонки, номера страниц. Объём 0.68 ГБ, покрытие краской 4–26% (текст на месте, разброс по вёрстке — нормально).

**Следующий шаг:** задача 1.5 (анализ DESCAN-архивов — первое касание) → 1.6 (генератор 10k/1k).

---

## 2026-09-08 — Задача 1.1 DONE: 2000 фото Unsplash

**Контекст:** Полный объём GT-фото для 1.6 (11k патчей, ~70% фото / ~30% документы из 1.2).

**Решение/Результат:** `Data/gt/unsplash/` — 2000 фото (w=2560, q=95), битых 0/2000, `manifest.jsonl` (photo_id, URL, размеры; seed 7 — выбор воспроизводим). Спот-чек периодики на 10 фото: пики до 300× среднего — это контентная периодика (фасады/текстуры), допустима; систематическая отбраковка «грязного» GT — в задаче 1.6. DIV2K решено не качать: 2000 фото × ~10–15 патчей ≈ 25k кандидатов, объёма хватает с запасом.

**Следующий шаг:** задача 1.2 — рендер документов.

---

## 2026-09-08 — Шаг 2 готов: каскад «Сканер», муар рождается алиасингом (задача 1.4 DONE)

**Контекст:** Этап 1, задача 1.4. Вход — оттиск из 1.3, выход — скан 300/600 DPI.

**Решение/Результат:** `ml/simulator/config.py` (ScanConfig: поворот 0.1–2°, PSF σ 0.3–1.2, Гаусс 0.004–0.012, Пуассон пик 40–120, пыль), `ml/simulator/scan.py` (`sample_scan_params`, `rotate_rgb`, `_add_dust`, `simulate_scan`), `ml/simulator/preview_scan.py`, мишень `flat` в `targets.py`.
- Порядок физически корректный: поворот → PSF в печатном пространстве (σ×S) → BOX-даунсэмпл → шумы → пыль.
- Доказательство муара на плоском поле (magazine 133 LPI, скан 300 DPI): lowband-энергия 0.051 при 0° → 0.072 при 0.5° → **0.194 при 1.5° (×3.8)**; пики растра на 0.443 цикл/px (теория 133/2400×8 = 0.443 ✓). Муар рисуется только ресемплингом, постфактум — ничего.
- На фото lowband-метрика тонет в контенте (ожидаемо) — для контента контроль визуальный: `ml/simulator/_scan/`.
- Важно для 1.6: генератор пар обязан поворачивать GT тем же углом (`rotate_rgb`), иначе пары разъедутся.

**Артефакты:** `ml/simulator/scan.py`, `preview_scan.py`, `ml/simulator/_scan/`.

**Следующий шаг:** задача 1.1 (докачка Unsplash до полного объёма) + 1.2 (рендер документов) → 1.6 (генератор 10k/1k).

---

## 2026-09-08 — Шаг 1 готов: каскад «Печать» AM (задача 1.3 DONE)

**Контекст:** Этап 1, задача 1.3. Только AM-растр, GT = Unsplash dev (20 фото, `Data/gt/dev/` + `manifest.jsonl`). Архивы DESCAN-18K не тронуты. Метаданные Unsplash (`photos.tsv000`) читаются из zip в память; из 25 000 строк отобрано 24 028 featured-фото с меньшей стороной ≥2000 px.

**Решение/Результат:** `ml/simulator/` — `config.py` (PrintConfig, пресеты newspaper 95 / magazine 133 / fine 165 LPI, углы C15/M75/Y0/K45 ±3°, TVI-гейн, `for_scan_dpi()`), `color.py` (RGB→CMYK), `screen.py` (ячейка, поворот, круг/эллипс/квадрат, построчная обработка чанками), `compose.py` (композит, оптический blur, бумага), `targets.py` (процедурные мишени), `preview.py` (верификация + бенчмарк).
- Пики 2D FFT синтетики на расчётных частотах: newspaper/fine/magazine, ошибка <1% (напр. 0.0552 против теории 0.0554 цикл/px; доминирует K-канал 45°).
- Тонопередача монотонна 0→0.97, полный диапазон, видимый dot gain.
- Бенчмарк CPU (патч 512): x4 ≈7 с, x8 ≈29 с. `oversample = print_dpi / scan_dpi` (x8 для 300 DPI, x4 для 600 DPI); печатное пространство 2400 DPI eq. — ячейка 14–28 px, достаточно для форм точек. 4800 DPI отложено (×4 цена без необходимости).
- Превью (scan-view, кропы 100%, спектры): `ml/simulator/_preview/`, `ml/simulator/_preview2/`.

**Артефакты:** `ml/simulator/*.py`, `ml/dataset/unsplash.py`, `Data/gt/dev/` (20 фото).

**Следующий шаг:** задача 1.4 — каскад «Сканер» (поворот, PSF, ресемплинг, шумы; муар — алиасингом).

---

## 2026-09-08 — Стратегия данных проекта (решение)

**Контекст:** Выбор источников обучающих данных для нейросетевого дескрининга. В проекте лежит DESCAN-18K (18 360 реальных пар скан/оригинал).

**Решение/Результат:**
1. **DESCAN-18K исключён из обучения.** Причины: (а) сканы с офисных МФУ (Canon imageRUNNER, Plustek OpticBook) не соответствуют продакшн-входу (планшетные/барабанные сканеры печатной продукции с выраженным растром и сканерным муаром); (б) GT выровнен AKAZE — не пиксельно-точный, потолок SOTA на нём ~23 dB PSNR; (в) лицензия CC BY-NC-SA — некоммерческая, а статус продукта не определён → закладываем коммерчески чистое ядро.
2. **Обучающее ядро — синтетика:** Unsplash Lite + DIV2K (фото) + собственный рендер документов (текст от 4 pt, 600 DPI; готовых датасетов с таким GT нет).
3. **Симулятор — двухкаскадный:** «виртуальная печать» в эквиваленте 2400–4800 DPI → «виртуальный сканер» (поворот 0.1–2°, PSF, ресемплинг до 300/600 DPI). Муар должен возникать как алиасинг при ресемплинге, а не накладываться постфактум. Критический режим: 150 LPI @ 300 DPI → период растра 2 px (граница Найквиста).
4. **DESCAN-18K — только референс дефектов:** измерить спектральные портреты сканов из Valid для калибровки симулятора (задача 1.5). Train.zip (35 ГБ) не распаковывать.
5. **DSP v3.7 (текущий браузерный продукт)** — тройная роль: baseline-планка качества; детектор остаточных периодических пиков (σ-thresholding в 2D FFT) как метрика на реальных сканах без GT; источник отлаженных алгоритмов (tiling + Hann overlap-add, FFT-визуализация, пресеты типов печати) для портирования в Rust/фронтенд.
6. **Метрики:** PSNR > 34 dB / SSIM > 0.95 — только на отложенной синтетике. На реальных сканах — энергия остаточных пиков спектра + качественная оценка vs v3.7.

**Артефакты:** `AGENTS.md`, `WORKFLOW.md`, `TASKS.md`, `JOURNAL.md`, `knowledge/`.

**Следующий шаг:** Этап 1 — задача 1.3/1.4 (симулятор «печать → сканер»).
